version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: survey_ai_api
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped